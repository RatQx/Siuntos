@model AutoNuoma.Models.Darbuotojas

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row justify-content-md-center">
    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                
            </div>
            <br />
            <br />
            <br />
            <br />
            <br />
            <br />
            <br />
            <div class="card-body">
                @using (Html.BeginForm("Login", "Darbuotojas", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        @Html.LabelFor(m => m.vardas)
                        @Html.TextBoxFor(m => m.vardas, "", new { @class = "form-control", @placeholder = "Prisijungimo vardas" })

                    </div>
                    <div class="form-group">
                        @Html.LabelFor(m => m.pavarde)
                        @Html.PasswordFor(m => m.pavarde, new { @class = "form-control", @placeholder = "Slaptazodis" })
                    </div>
                    <div class="form-group">
                        <input type="submit" name="submit" class="btn btn-primary" value="Login" />
                    </div>
                }
            </div>
        </div>
    </div>
</div>